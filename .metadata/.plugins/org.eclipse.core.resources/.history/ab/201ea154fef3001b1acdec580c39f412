
public class SortedSquare {
	public int[] sortedSquares(int[] nums) {
		int [] anotherArray = new int[nums.length];
		for (int i = 0 ;i<nums.length;i++) {
			anotherArray[i] = nums[i] *nums[i];
		}
		for(int i = 0;i<anotherArray.length-1;i++) {
			int min = i;
			for(int j = i+1;j<anotherArray.length;j++) {
				if(anotherArray[min] > anotherArray[j]) {
					min = j;
				}
			}
			int temp = anotherArray[min];
			anotherArray[min] = anotherArray[i];
			anotherArray[i] = temp;
		}
		
		return anotherArray;
	}

	public static void main(String[] args) {
		SortedSquare sorted = new SortedSquare();
		
		int[] nums = {4,8,3,2,5,6,7};
		  int[] sortedSquares = sorted.sortedSquares(nums);
		 for(int i = 0;i<sortedSquares.length;i++) {
			 System.out.println(sortedSquares[i]);
		 }

		
		
		
		
	}

}
